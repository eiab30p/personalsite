version: 0.1

phases:
  # build:
  #   commands:
  #     - npm install
  #     - npm run build
  pre_build:
    commands:
      - echo Installing npm dependencies...
      - npm install
  build:
    commands:
      - echo Starting Build
      - npm run build

artifacts:
  files:
    - "**/*"
  base-directory: build
  discard-paths: yes
